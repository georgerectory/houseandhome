# Property archive

An archived property's folder moves here from `docs/properties/`, with
its README headed `ARCHIVED <date> - <reason>`. Nothing in this folder
feeds a default view; it is read only for a cross-property comparison.
A purged property is deleted from here too.

Currently empty.
